// This app is a client-side SPA (all data loads in the browser via /api/*
// endpoints), so server-side rendering is disabled. Page options must live in
// this module rather than in +page.svelte.
export const ssr = false;
